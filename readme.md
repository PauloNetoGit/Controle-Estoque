# bibliotetas usadas

Tkinter (para a interface gráfica)
SQLite (para o banco de dados)
Pillow (para manipulação de imagens de etiquetas, caso você queira gerar códigos de barras)
ReportLab (para gerar PDF, caso precise para a impressão de etiquetas)

<code>pip install pillow reportlab</code>